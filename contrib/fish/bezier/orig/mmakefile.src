include $(TOP)/config/make.cfg

TARGET_CFLAGS := $(filter-out -Werror-implicit-function-declaration,$(TARGET_CFLAGS))

%build_prog mmake=fish-bezier1 \
    progname=bezier1 targetdir=$(CONTRIBDIR)/fish \
    files=bezier uselibs="arossupport amiga arosc m"

