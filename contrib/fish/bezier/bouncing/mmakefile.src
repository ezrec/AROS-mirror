include $(TOP)/config/make.cfg

CFLAGS := $(filter-out -Werror-implicit-function-declaration,$(CFLAGS))

%build_prog mmake=fish-bezier2 \
    progname=bezier2 targetdir=$(CONTRIBDIR)/Misc/fish \
    files=bezier
