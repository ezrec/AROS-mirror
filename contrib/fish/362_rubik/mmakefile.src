include $(TOP)/config/make.cfg

FILES := rubik getwindow makecube rotate trig gadgets draw extras solve \
	blurb

%build_prog mmake=fish-362_rubik \
    progname=362_rubik targetdir=$(CONTRIBDIR)/Misc/fish \
    files=$(FILES)
