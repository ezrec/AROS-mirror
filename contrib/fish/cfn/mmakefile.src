include $(TOP)/config/make.cfg

#MM fish-cfn : fish-cfn_newshell

%build_prog mmake=fish-cfn \
    progname=cfn targetdir=$(CONTRIBDIR)/fish \
    files=cfn uselibs="arossupport amiga arosc m"

%build_prog mmake=fish-cfn_newshell \
    progname=cfn_newshell targetdir=$(CONTRIBDIR)/fish \
    files=cfn_newshell uselibs="arossupport amiga arosc m"
