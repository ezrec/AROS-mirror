#   $Id$
include $(TOP)/config/make.cfg

FILES := nforce handler unit

%build_module mmake=contrib-networking-drivers-nforce \
    modname=nforce modtype=device \
    files="nforce_init $(FILES)" \
    moduledir=Devs/Networks uselibs="rom amiga hiddstubs"

%common
