include $(TOP)/config/make.cfg

#MM contrib-ptlay-lib : includes linklibs

LIB := ptplay
FILES := ptinit ptplay ptplaytest

USER_CFLAGS := -DAMIGA -Wno-parentheses -Wno-missing-braces

%build_module mmake=contrib-ptplay-lib modname=$(LIB) modtype=library \
  files=$(FILES) uselibs="amiga rom m"

%copy_includes mmake=includes-copy includes=ptplay.h path=libraries

%common
