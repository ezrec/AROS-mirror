#   $Id$
#
#   Makefile to make AMosaic.

include $(TOP)/config/make.cfg

FILES := globals \
	Boingtransfer \
	socketbase \
	version \
	spinner \
	picread \
	mo-www \
	locale \
	globalhist \
	fonts \
	TextEditField \
	TO_HTML \
	NewGroup \
	HTMLparse \
	HTMLlists \
	HTML_img \
	HTMLformat \
	HTMLimages \
	HTML \
	gui-menubar \
	main \
	gui-dialogs \
	gui-documents \
	history \
	htmlamiga \
	gui \
	mui \

# These are Motif?
#	hotlist \
# ReXX is not yet ported to AROS
#	rexx
#

EXEDIR := $(AROSDIR)/AMosaic
EXE := $(EXEDIR)/AMosaic

USE_AROSLIB := yes
USE_CLIB_SHARED := yes
USE_AMIGALIB := yes
USE_MUILIB := yes

USER_INCLUDES := -I../../mui/libs/muimaster -I$(INCDIR)/libwww2
PRELIBS := $(PRELIBS) -lwww2
#MM mui-amosaic : includes linklibs 

%make_prog_setup mmake=mui-amosaic

mui-amosaic : setup $(EXE)

%make_prog_multi dir=$(EXEDIR) target=$(EXE)

%common
%include_deps
