# $Id$
#
# Makefile for the C: Programs

include $(TOP)/config/make.cfg

FILES  := Ping

EXEDIR := $(CONTRIBDIR)/Network
EXE    := $(EXEDIR)/Ping

USE_CLIB_SHARED := yes
USE_AMIGALIB    := yes
USE_MLIB        := yes
USE_AROSLIB     := yes

#MM- workbench : workbench-c
#MM workbench-c : includes linklibs network-includes

%make_prog_setup mmake=workbench-c

workbench-c : setup $(EXE)

%make_prog_multi dir=$(EXEDIR) target=$(EXE)

%common
%include_deps
