#   $Id$
#
#   Makefile to make viewofont.

include $(TOP)/config/make.cfg

FILES := bulletmainfile engine view

EXEDIR := $(AROSDIR)/Freedom
EXE := $(EXEDIR)/viewofont

USE_AROSLIB := yes
USE_CLIB_SHARED := yes
USE_AMIGALIB := yes
USE_MLIB := yes

#MM freedom-test : includes linklibs

%make_prog_setup mmake=freedom-test

freedom-test : setup $(EXE)

%make_prog_multi dir=$(EXEDIR) target=$(EXE)

%common
%include_deps
