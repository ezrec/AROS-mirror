
To see what file is used for what class, just see the makefile...



For the NList class :

To find in which .c file are NList functions you can look in NList_protos.h
The dispatcher is in NList_mcc0.c
Most standard MUI methods are in NList_mcc.c and NList_mcc1.c

blob
