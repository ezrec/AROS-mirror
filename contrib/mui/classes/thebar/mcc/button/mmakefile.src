#   $Id$

include $(TOP)/config/make.cfg

#MM contrib-zune-classes-thebbutton-mcc : includes linklibs

FILES := library \
	 brc1 \
	 build \
	 class \
	 Debug \
	 scale \
	 utils

USER_INCLUDES := -I../../mcp -I../../include/aros

%build_module mmake=contrib-zune-classes-thebutton-mcc \
  modname=TheButton modtype=mcc moduledir=Extras/Zune/MCC_TheBar/Classes/Zune \
  files=$(FILES) \
  uselibs="amiga mui rom"

%common
