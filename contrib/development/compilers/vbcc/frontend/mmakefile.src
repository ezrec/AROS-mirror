include $(TOP)/config/make.cfg

FILES = vc

EXEDIR := $(CONTRIBDIR)/vbcc
EXE    := $(EXEDIR)/vc

USE_CLIB_SHARED := yes

USER_CFLAGS := -O2

#MM- contrib-development-compilers-vbcc-frontend : frontend
#MM
frontend : setup $(EXE)

$(OBJDIR)/%.o: %.S
	%compile_q opt=$(USER_CFLAGS)

%make_prog_setup mmake=frontend
%make_prog_multi dir=$(EXEDIR) target=$(EXE)

%common
%include_deps

