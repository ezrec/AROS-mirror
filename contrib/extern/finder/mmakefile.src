# Metamakefile for Finder for AROS

include $(TOP)/config/make.cfg

# Use "make contrib-extern-finder" to download and unpack Finder from
# archives.aros-exec.org. Run "make" to build Finder. This double make is
# necessary because the archive has an embedded mmakefile.src.

ORIGIN  := http://archives.aros-exec.org/share/utility/filetool
ARCHIVE := finder.src-aros

#MM- contrib : workbench-utilities-finder

#MM
contrib-extern-finder :
	%fetch archive_origins=$(ORIGIN) archive=$(ARCHIVE) suffixes=zip \
	location=$(PORTSSOURCEDIR)/archives.aros-exec destination=$(TOP)/extern
	@$(ECHO) "Run 'make' to build Finder"

%common
