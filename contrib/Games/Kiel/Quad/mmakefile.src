include $(TOP)/config/make.cfg

FILES := Quad
DESTDIR :=$(CONTRIBDIR)/Games/Kiel
EXES := $(foreach f,$(FILES), $(DESTDIR)/$(f))
INFO := $(foreach f,$(FILES), $(DESTDIR)/$(f).info)

USE_CLIB_SHARED := yes
USE_AMIGALIB := yes
USE_MLIB := yes
USE_AROSLIB := yes

#MM- contrib-games : games-kiel-quad

%make_prog_setup mmake=games-kiel-quad

#MM
games-kiel-quad : setup $(EXES) $(INFO)

$(DESTDIR)/%.info : %.info
	$(CP) $? $(DESTDIR)

%make_prog_single dir=$(DESTDIR)

%common
%include_deps
