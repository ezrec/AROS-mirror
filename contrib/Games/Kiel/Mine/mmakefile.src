include $(TOP)/config/make.cfg

FILES := Mine
DESTDIR := $(CONTRIBDIR)/Games/Kiel
EXES := $(DESTDIR)/Mine
INFO := $(foreach f,$(FILES), $(DESTDIR)/$(f).info)

USE_CLIB_SHARED := yes
USE_AMIGALIB := yes
USE_MLIB := yes
USE_AROSLIB := yes

#MM- contrib-games : games-kiel-mine

%make_prog_setup mmake=games-kiel-mine

#MM
games-kiel-mine : setup $(EXES) $(INFO)

$(DESTDIR)/%.info : %.info
	$(CP) $? $(DESTDIR)

%make_prog_single dir=$(DESTDIR)

%common
%include_deps
