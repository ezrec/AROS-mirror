include $(TOP)/config/make.cfg

FILES = cpuid


EXEDIR := $(CONTRIBDIR)/gnu
EXE    := $(EXEDIR)/cpuid

USE_CLIB := yes
USE_MLIB := yes

USER_CFLAGS := -O2

#MM- contrib-gnu-cpuid : cpuid
#MM- contrib-gnu      : cpuid
#MM
gzip : setup $(EXE)

$(OBJDIR)/%.o: %.S
	%compile_q opt=$(USER_CFLAGS)

%make_prog_setup mmake=cpuid
%make_prog_multi dir=$(EXEDIR) target=$(EXE)

%common
%include_deps

