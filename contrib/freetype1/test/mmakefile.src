#   $Id$
#
#   Makefile to make Rexx.

include $(TOP)/config/make.cfg

EXEDIR := $(CONTRIBDIR)/freetype1

#MM- contrib-freetype1-test : \
##MM      contrib-freetype1-test-ftdump \
#MM      contrib-freetype1-test-fterror \
#MM      contrib-freetype1-test-ftlint \
#MM      contrib-freetype1-test-ftmetric \
#MM      contrib-freetype1-test-ftsbit \
#MM      contrib-freetype1-test-ftstring \
#MM      contrib-freetype1-test-ftstrpnm \
#MM      contrib-freetype1-test-ftstrtto \
#MM      contrib-freetype1-test-fttimer \
#MM      contrib-freetype1-test-ftview \
#MM      contrib-freetype1-test-ftzoom


USER_INCLUDES := -I. -I../lib -I../lib/extend -I../lib/arch/amigaos #-Iarch/amigaos

GFILES := gmain display blitter arch/amigaos/gw_amiga

%build_prog mmake=contrib-freetype1-test-ftzoom \
    progname=ftzoom targetdir=$(EXEDIR) \
    files="$(GFILES) ftzoom common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftview \
    progname=ftview targetdir=$(EXEDIR) \
    files="$(GFILES) ftview common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftlint \
    progname=ftlint targetdir=$(EXEDIR) \
    files="ftlint common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftdump \
    progname=ftdump targetdir=$(EXEDIR) \
    files="ftdump common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftstring \
    progname=ftstring targetdir=$(EXEDIR) \
    files="$(GFILES) ftstring common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-fttimer \
    progname=fttimer targetdir=$(EXEDIR) \
    files="$(GFILES) fttimer common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftsbit \
    progname=ftsbit targetdir=$(EXEDIR) \
    files="ftsbit common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftstrpnm \
    progname=ftstrpnm targetdir=$(EXEDIR) \
    files="ftstrpnm common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftmetric \
    progname=ftmetric targetdir=$(EXEDIR) \
    files="ftmetric common" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-ftstrtto \
    progname=ftstrtto targetdir=$(EXEDIR) \
    files="$(GFILES) ftstrtto common arabic" \
    uselibs="freetype1 amiga arosc m"

%build_prog mmake=contrib-freetype1-test-fterror \
    progname=fterror targetdir=$(EXEDIR) \
    files="fterror" \
    uselibs="freetype1 amiga arosc m"

%common
