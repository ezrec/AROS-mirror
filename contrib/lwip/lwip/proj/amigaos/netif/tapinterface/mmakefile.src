# $Id$
include $(TOP)/config/make.cfg

FUNCS	:= \
	opentapinterface \
	readtapinterface \
	waittapinterface \
	writetapinterface

#MM- lwip-tapinterface-kobj : includes linklibs

USER_CFLAGS := -I$(SYS_INCLUDES) -I$(GENINCDIR)

%build_module mmake=lwip-tapinterface \
    modname=tapinterface modtype=library \
    files=$(FUNCS) uselibs=autoinit usehostlibs=c \
    compiler=host
