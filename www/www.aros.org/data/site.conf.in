# Per-site stuff for the Apache config
#
# Where the files are installed. This is usually /home/user/www/www.aros.org/
# The default is the parent of the current directory. If this variable is not
# set, then the various scripts try to figure this out themselves.
#BASEDIR=

# Comment these definitions out if you don't have them. Then the graphs
# for the size of the repositories will not be generated.
GNUPLOT=gnuplot # gnuplot
CONVERT=convert # ImageMagick

# The name of the virtual host on which AROS is hosted. To create a local
# copy for testing purposes, add this line to /etc/hosts:
#
#   127.0.0.1       localhost www2.aros.org
#
# (append www2.aros.org to the line which begins with 127.0.0.1).
# If you're using a proxy, you must add www2.aros.org to the sites
# for which your browser should not use a proxy.
HOSTNAME=www2.aros.org

# Where mailman ist installed. Don't worry if it isn't installed, you
# don't need it (you will just not be able to test the mailing lists).
MAILMAN=/var/lib/mailman

# In which directory should the files be installed ?
HTMLDIR=../www

# Destination for the screenshots (where the thumbnails should be
# generated, etc.)
SCREENSHOTDIR=$HTMLDIR/pics/screenshots

# The directory which contains the files for the AROS FTP server.
# If this doesn't exist, then an empty download page will be
# generated.
FTPDIR=/home/ftp/pub/aros

# Where the AROS sources are installed
AROSBASE=$HOME/packages/AROS

# Where is the AROS repository ?
AROSDIR=$AROSBASE/AROS

# Where is the binaries repository (screenshots, etc) ?
BINDIR=$AROSBASE/binaries

# Where is the contrib repository ?
CONTRIBDIR=$AROSBASE/contrib


