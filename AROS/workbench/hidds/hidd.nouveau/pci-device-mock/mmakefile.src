#   $Id$
include $(TOP)/config/make.cfg

FILES	:= pcimockdriverclass

USER_INCLUDES := -I$(SRCDIR)/rom/hidds/hidd.pci/

%build_module mmake=hidd-pci-mock modname=pcimock modtype=hidd \
    files=$(FILES) uselibs="oop rom amiga"
