The 'loop' network stack is only for testing purposes.

It provides two network interfaces 'lo0' and 'lo1' that
will serve connection on any protocol to the local machine.

This is mainly as a 'functional skeleton' example of how
to create an ASocketBase compatable library.

