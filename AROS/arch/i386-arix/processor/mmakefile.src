# $Id$
#
include $(SRCDIR)/config/aros.cfg

USER_CFLAGS := -I$(SRCDIR)/rom/processor

FILES := \
    processor_init 

%build_archspecific mainmmake=kernel-processor maindir=rom/processor \
    arch=arix-i386 files="$(FILES)"

%common
